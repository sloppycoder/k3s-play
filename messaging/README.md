# messaing components

```shell
# redpanda does not work on my setup. however it does work on Rancher Desktop on M1 Macbook Air 
# (which curious also uses K3S and ARM64 CPU)
kubectl apply -k redpanda

# install nats jet streaming
kubectl apply -k nats

```
